interface:
  display_name: "selfL Distill"
  short_description: "Self-learning loop, durable rule extraction, and skill health checks"
  default_prompt: "Use $selfl-distill to convert this completed work into reusable rules that improve future agent runs."
