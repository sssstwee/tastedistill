interface:
  display_name: "TasteD Debug"
  icon: "../../../assets/distill-icon.svg"
  composer_icon: "../../../assets/distill-icon.svg"
  short_description: "Root-cause diagnosis, regressions, runtime evidence, and minimal fixes"
  default_prompt: "Use $tasted-debug to reproduce the failure, prove root cause, apply a minimal fix, and verify it."
